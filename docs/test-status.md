# Upstream Perl Test Status

Status values:

- `enabled`: runs in CI through `scripts/run-bcftools-test-pl.sh`.
- `rust-covered`: Rust-native tests cover a meaningful local slice, but the
  upstream Perl function is not enabled in CI yet.
- `not-yet-ported`: upstream test exists, but the required Rust subcommand or
  shared infrastructure is not complete enough to enable it.
- `partial-rust-covered`: the Rust command has meaningful unit/integration
  coverage, but the full upstream Perl slice is not enabled yet.
- `blocked-local-tool`: the Rust subcommand has partial coverage, but this
  test function also requires external tools not yet provided by CI.

| `test.pl` function | Status | Notes |
| --- | --- | --- |
| `test_vcf_head` | enabled | Plain VCF `bcftools head`; CI runs it in the enabled parity slice regex. |
| `test_vcf_head2` | enabled | Compressed VCF and BCF-pipe `head` coverage runs with Rust-backed staged `bgzip`/`tabix` harness helpers. |
| `test_index` | enabled | VCF/BCF indexing, explicit output path, and streaming index creation pass in CI. |
| `test_vcf_idxstats` | enabled | `bcftools index -s/-n` over TBI/CSI, VCF.gz, BCF, and direct CSI paths passes in CI. |
| `test_vcf_sort` | rust-covered | Rust integration coverage covers coordinate sorting, output formats, write-index, temp-prefix/compression-level shape, and Kestrel header compatibility; full Perl slice still depends on broader `query` parity. |
| `test_vcf_view` | rust-covered | Rust integration coverage includes VCF/VCF.gz/BCF I/O, sample/region/target filtering, common site filters, genotype/phasing filters, expressions over core/INFO fields, repeated `-A` symbolic reference ALT trimming with byte-for-byte `merge.gvcf.2.a.{1,2}.out` coverage, genotype-driven `-a` ALT trimming with byte-for-byte `many.alleles.trim.out` coverage, output formats, threading, and Kestrel headers; full Perl parity remains incomplete for advanced FORMAT/sample expression and structured writer semantics. |
| `test_csq` | not-yet-ported | `csq` not ported. |
| `test_csq_real` | not-yet-ported | `csq` not ported. |
| `test_gtcheck` | not-yet-ported | `gtcheck` not ported. |
| `test_mpileup` | not-yet-ported | `mpileup` not ported. |
| `test_naive_concat` | rust-covered | Rust integration coverage includes `--naive` and `--naive-force`; Perl slice not enabled yet. |
| `test_plugin_scatter` | rust-covered | Rust integration coverage matches all three upstream scatter fixtures; Perl slice is not enabled in CI yet. |
| `test_plugin_split` | rust-covered | Rust integration coverage matches the filter-free upstream split fixtures and covers `-k` tag projection plus `-Oz` BGZF VCF output; rows using `-i 'GT="alt"'` remain blocked on the filter engine. |
| `test_plugin_vrfs` | not-yet-ported | Plugin not ported. |
| `test_rename_chrs` | not-yet-ported | Depends on `annotate`/`query`. |
| `test_roh` | not-yet-ported | `roh` not ported. |
| `test_tabix` | enabled | VCF BGZF indexing/querying runs with Rust-backed staged `bgzip`/`tabix` harness helpers. |
| `test_trio_dnm3` | not-yet-ported | Plugin not ported. |
| `test_usage` | not-yet-ported | Harness requires `IO::Pty`; full command table is not ported. |
| `test_vcf_64bit` | not-yet-ported | Depends on broader VCF command coverage. |
| `test_vcf_annotate` | rust-covered | Rust integration coverage includes chromosome renaming, simple `--rename-annots` INFO/FILTER/FORMAT ID renaming with byte-for-byte `annotate29.out` coverage, `-x` removal for ID/QUAL/FILTER/INFO/FORMAT tags, FILTER/INFO/FORMAT keep-only removal, PASS header injection/preservation for removal slices, `--force` command-shape compatibility, byte-for-byte `annotate6.out`, `annotate7.out`, `annotate25.out`, and `annotate28.out` fixture coverage, VCF/VCF.gz/BCF input, output formats, and Kestrel reads; annotation transfer, header injection, column mapping, overlap modes, and full upstream parity remain. |
| `test_vcf_call` | not-yet-ported | `call` not ported. |
| `test_vcf_call_cAls` | not-yet-ported | `call` not ported. |
| `test_vcf_check` | rust-covered | Rust `stats` has integration coverage for the local stats slice; the upstream check Perl flow is not enabled yet. |
| `test_vcf_check_merge` | not-yet-ported | Depends on `stats`/`merge`. |
| `test_vcf_concat` | rust-covered | Rust integration coverage includes same-sample concat, regions, duplicate removal, output formats, indexing, headers, threads, and Kestrel reads; ligation and full synced-reader edge cases remain. |
| `test_vcf_consensus` | not-yet-ported | `consensus` not ported. |
| `test_vcf_consensus_chain` | not-yet-ported | `consensus` not ported. |
| `test_vcf_convert` | rust-covered | Rust integration coverage includes TSV/23andMe-style conversion, gVCF expansion, Oxford GEN/HAP/HAP-LEGEND forward and reverse paths, sample selection, filters, output formats, indexing, and many upstream fixtures; full edge-case parity remains. |
| `test_vcf_convert_gvcf` | rust-covered | Rust integration coverage includes VCF/VCF.gz/BCF gVCF expansion and filter-gated expansion behavior; Perl slice not enabled yet. |
| `test_vcf_convert_hls2vcf` | rust-covered | Rust integration coverage includes HAP/LEGEND/SAMPLE back-conversion, text/VCF.gz/BCF output, indexing, and fixture parity for the current slice; haploid-missing BCF serialization remains dependency-blocked. |
| `test_vcf_convert_hs2vcf` | rust-covered | Rust integration coverage includes HAP/SAMPLE back-conversion, sample selection, sex/haploid2diploid handling, output formats, indexing, and fixture parity for the current slice; haploid-missing BCF serialization remains dependency-blocked. |
| `test_vcf_convert_tsv2vcf` | rust-covered | Rust integration coverage includes explicit-column TSV, AA/reference-derived alleles, GT sample fields, skipped malformed rows, diagnostics counters, output formats, and indexing; full 23andMe edge-case parity remains. |
| `test_vcf_filter` | rust-covered | Rust integration coverage includes VCF/VCF.gz/BCF I/O, expression filtering over the current shared filter slice, upstream single `&`/`|` FORMAT-vector site predicates (`filter.2.out`), bcftools GT class literals (`filter.{12,13,14,15,16,17,18,19}.out`), FORMAT sample/value subscripts (`filter.{20,21,22,23,24,25}.out`), missing QUAL comparison (`filter.26.out`), SnpGap type-list matching for symbolic deletion spans (`filter.29.out` text path), FORMAT/AO aggregate `-S .` behavior (`filter.{30,31,32,33,34,36}.out` text path), soft/hard FILTER rewriting, masks, gap filters, set-GTs, regions/targets, output formats, indexing, headers, threads, and Kestrel reads; full FORMAT/sample-vector parity remains. |
| `test_vcf_isec` | rust-covered | Rust integration coverage includes pairwise intersections, collapse modes, record/target filters, directory output, output formats, indexing, and Kestrel reads; full synced-reader parity remains. |
| `test_vcf_isec2` | rust-covered | Rust integration coverage covers the current multi-file/directory-output slice; full upstream parity remains. |
| `test_vcf_merge` | rust-covered | Rust integration coverage includes same-site sample-column concatenation, simple sorted text-mode exact-site union with missing FORMAT sample values, sites-only same-locus ALT union with AC/AN summing and byte-for-byte `merge.8.out` coverage, sampled plus sites-only same-locus ALT union with byte-for-byte `merge.9.1.out` coverage, narrow `-i AN:sum,AC:sum` INFO-rule summing for that sampled/sites-only ALT-union slice with byte-for-byte `merge.9.2.out` coverage, narrow `-i AF:join` INFO-rule joining for sites-only ALT-union records with byte-for-byte `merge.join.1.out` coverage, repeated `--force-samples` duplicate-prefixing with byte-for-byte `merge.9.out` coverage, broader upstream `merge --force-samples` text parity with byte-for-byte `merge.abc.out`, `merge.abc.2.out` (`-Fx`), and `merge.abc.3.out` (`-0`) coverage, narrow `-m none` same-position conflict retention with byte-for-byte `merge.10.1.out` coverage, narrow sampled same-position allele unions for `-m both` and `-m snp-ins-del` with byte-for-byte `merge.10.{2,3}.out` coverage, broader duplicate-sample `-m none|both|all` parity with byte-for-byte `merge.2.{none,both,all}.out` coverage, sampled same-ref ALT-subset merge for `--merge none|both` with byte-for-byte `merge.multiallelics.1.1.out` coverage, sampled star-allele REF-extension merge with byte-for-byte `merge.5.out` coverage, sampled same-position INFO Number=A/R/G remapping and summing with byte-for-byte `merge.3.out` coverage, ID-paired sampled same-position vector merging with byte-for-byte `merge.4.out` coverage, FORMAT-key union plus Number=A/R/G remapping with byte-for-byte `merge.6.out` coverage, default-mode sampled same-position allele union with FORMAT/AD vector expansion and byte-for-byte `merge.11.1.out` coverage, narrow `<NON_REF>` symbolic allele union for `--merge none|both` with byte-for-byte `merge.12.1.out` coverage, highest-input VCF fileformat preservation with byte-for-byte `merge.symbolic.1.1.out` coverage, byte-for-byte `merge.noidx.abc.out` coverage including `--no-index`, single-input `--force-single` byte-for-byte `merge.LPL.0.out` coverage, multi-input LPL `--force-samples` byte-for-byte `merge.LPL.1.out` coverage, narrow `merge --force-samples -L 1` local-alleles FORMAT vector localization with byte-for-byte `merge.LPL.2.out` coverage, narrow `merge --force-samples -L 2` local-alleles FORMAT vector localization including absent-sample `LAA` compaction with byte-for-byte `merge.LPL.3.out` coverage, higher-threshold local-alleles vector localization with byte-for-byte `merge.LPL.{4,5,6}.out` coverage, phased-record byte-for-byte `merge.phased.1.1.out` coverage, narrow exact-site reference-block handling with byte-for-byte `merge.broken-gvcf.1.out` coverage, narrow gVCF reference-symbolic same-position handling with byte-for-byte `merge.gvcf.10.{1,2}.out` coverage, narrow reference-backed gVCF block splitting with byte-for-byte `merge.gvcf.10.{3,4}.out` coverage, FASTA-backed gVCF region-boundary splitting and filtering with byte-for-byte `merge.gvcf.9.{1,2,3,4}.out` coverage, no-reference gVCF three-input default and `-m both,*|both,**` reference-symbolic trim fixture parity with byte-for-byte `merge.gvcf.2{,.1,.2}.out` coverage, no-reference gVCF REF-extension `<NON_REF>` row merging plus default and `-M AD:.,PL:.` AD/PL missing fills with byte-for-byte `merge.mrules.1.{1,2}.out` coverage, single-base gVCF reference-block `END=POS` suppression with byte-for-byte `merge.gvcf.7.out` coverage, no-reference gVCF block splitting around overlapping records with byte-for-byte `merge.gvcf.8.out` coverage, sampled no-reference gVCF same-position `<NON_REF>` ordering with byte-for-byte `merge.gvcf.5.1.out` coverage, existing no-reference gVCF three-input overlap fixture with byte-for-byte `merge.gvcf.11.1.out` coverage, `SRC:join` no-reference gVCF reference-block overlap merging with byte-for-byte `merge.gvcf.3.out` coverage, no-reference gVCF reference-block overlap splitting with single-site `<*>` records, later-input `##contig`/`##ALT` header preservation, and upstream gVCF INFO ordering with byte-for-byte `merge.gvcf.4.out` coverage, no-reference reference-block end-boundary splitting plus reference-row `AN` derivation with byte-for-byte `gvcf.merge.1.out` coverage, narrow `-0`/`--missing-to-ref` absent-input hom-ref fill with `AN` adjustment, duplicate-sample handling, file-list input, output formats, and Kestrel reads; 48 merge integration tests; general allele unification, complete merge-mode semantics, general INFO rules, full upstream `--missing-to-ref` fixture parity, broader missing-rules, broader gVCF, indexed regions, and upstream Perl parity remain. |
| `test_vcf_merge_big` | not-yet-ported | `merge` not ported. |
| `test_vcf_norm` | rust-covered | Rust integration coverage includes the current `-d`/`--rm-dup` duplicate-removal slice, `-i`/`--include` gating for duplicate removal including `ID=@file` and simple shared filter expressions, narrow `-c s` reference-check swap behavior, narrow `-m -` multiallelic splitting with `-S lex` and `--multi-overlaps .|0` GT projection, narrow filtered and unfiltered `-m +both` multiallelic joining for simple records including missing-ploidy GT projection and SNP/indel grouping, byte-for-byte `norm.rmdup.{1,2,3,4,5}.out`, `norm.rmdup.2.{1,2}.out`, `norm.rmdup.3.{1,2}.out`, `norm.filter.{1,2}.out`, `norm.check-ref.1.out`, `norm.sort.{1,2}.out`, `norm.split.5.1.out`, `norm.breakend.1.1.out`, `norm.5.{1,2}.out`, `norm.join-missing-ploidy.1.out`, and `norm.4.1.out` text fixture coverage, VCF/VCF.gz/BCF input, output formats, PASS header insertion/preservation, `-f` command-shape compatibility for duplicate removal and `-c s`, and narrow FASTA-backed left alignment for symbolic `<DEL>` records and simple one-base deletions before duplicate removal; full left alignment, full reference-check modes, general split/join multiallelics, atomization, BCF-pipe header-order parity, and full upstream parity remain. |
| `test_vcf_plugin` | partial-rust-covered | Static plugin registry/listing is in; `+counts`, `+missing2ref`, `+fill-AN-AC`, `+allele-length`, `+variant-distance`, `+check-ploidy`, `+tag2tag` (gl-to-pl/gl-to-gp/gp-to-gt/LXX-to-XX), `+add-variantkey`, `+variantkey-hex`, `+remove-overlaps`, `+af-dist`, `+smpl-stats`, `+indel-stats`, `+ad-bias`, `+prune`, `+dosage`, `+guess-ploidy`, `+contrast`, `+fixref`, `+trio-switch-rate`, `+trio-stats`, `+mendelian2`, `+parental-origin`, `+fixploidy`, `+GTsubset`, `+GTisec`, `+fill-from-fasta`, `+scatter`, `+split`, `+isecGT`, `+frameshifts`, and `+check-sparsity` have Rust implementations (32 of 41 on `main`). `+missing2ref` (`missing2ref.out`, plus `-p` phased and `-m` major-allele modes), `+fill-AN-AC`/`+allele-length`, all four `+variant-distance` modes, all three `+check-ploidy` fixtures, all six upstream `+tag2tag` fixtures (`view.PL.vcf`, `view.GL-GP.vcf`, `view.GT.vcf`, `tag2tag.LPL.1.{1,2,3}.vcf`), both VariantKey plugins (full `query.add-variantkey.vcf` / `variantkey-hex.out` fixtures), `+remove-overlaps` (all six `remove-overlaps.1.*` overlap/dup/`-O t`/`--reverse` fixtures), `+af-dist` (`af-dist.out`, HWE prob + AF-deviation histograms, `-d`/`-p` bin-definition files, plus `-l`/`--list` debug genotype output), `+smpl-stats` (`smpl-stats.1.out`, per-sample/per-site genotype stats), `+indel-stats` (`indel-stats.1.out`, SN/DVAF/DLEN/DFRAC/NFRAC), `+ad-bias` (`ad-bias.out`, Fisher exact test on FMT/AD, both inputs), `+prune` (`prune.1.{1,2,3,4,6}.out` + `prune.2.1.out`, windowed maxAF/1st pruning **and** `calc_ld` r2/LD'/RD `-a`/`-m`/`-f` modes), `+dosage` (`dosage.{1,2,3}.out`, PL/GL/GT dosages), `+guess-ploidy` (`guess-ploidy.{PL,GL}.out`, sex inference from PL/GL likelihoods), `+contrast` (`contrast.out`/`.1.out`/`.1.1.out`/`.1.2.out`, control/case association + novel allele/genotype), `+fixref` (`fixref.{4,5,6,7}.out`, FASTA REF/ALT ref-alt/swap/flip/flip-all), `+trio-switch-rate` (`trio.out`, PED-trio phase-switch rate), `+trio-stats` (`trio-stats.out`/`trio-stats.2.out`, Mendelian/DNM/transmitted trio stats), `+mendelian2` (`mendelian.{1,3,4,6,7,8}.out`, built-in GRCh37 ruleset → per-record ploidy/inheritance, count/annotate/delete/list modes), `+parental-origin` (`parental-origin.{1,2,3,4,5}.out`, DEL/DUP genotype-likelihood parental-origin models + local `kf_betai` binomial filters), `+fixploidy` (`fixploidy.out`, `ploidy.c`-subset region/sex ploidy query + bcf GT expand/in-place rewrite), `+GTsubset` (`view.GTsubset.{NA1,NA1NA2,NA1NA2NA3}.out`, exclusive-shared-genotype site filter on bcf-encoded allele/phase ints), `+GTisec` (`view.GTisec.{,H,Hm,Hmv,Hv,m,mv,v}.out`, genotype-intersection subset counts in banker's-sequence order), `+fill-from-fasta` (`ref.out` / `aa.2.out`, REF fill from a FASTA reference, `-N` non-ACGTN→N), `+scatter` (`scatter.1.{1,2,3}.out`, split into multiple VCFs by `-n` chunks or `-s`/`-x` regions), and `+split` (`split.1.{1,2,3,7}.out` / `split.2.1.out`, per-sample and sample/group-file VCF splitting plus `-k` tag projection and `-Oz` BGZF VCF output) match their upstream `*.out` fixtures byte-for-byte. `+isecGT`, `+frameshifts`, and `+check-sparsity` have no dedicated upstream fixtures and are covered by synthetic Rust integration tests for their core behavior. The umbrella Perl slice stays disabled until enough plugins land. |
| `test_vcf_query` | rust-covered | Rust integration coverage includes list-samples, sample selection, headers, regions/targets, record and sample filters, and many formatter tokens/functions; full shared formatter parity remains. |
| `test_vcf_regions` | rust-covered | Rust `query`/`view`/`filter`/`isec` have local region and target coverage, but full upstream region/target semantics are not complete. |
| `test_vcf_reheader` | rust-covered | Rust integration coverage includes header replacement, sample renaming, FAI contig updates, stdin, BCF output, in-place BCF, and threads; `test_rename_chrs` still depends on `annotate`/full `query`. |
| `test_vcf_stats` | rust-covered | Rust integration coverage includes the current stats report slice, region filtering, sample selection, two-input comparison, computed TYPE expressions, and upstream fixture checks; full stats parity remains. |
